module.exports = {
    playCmd = require('play'),
    skipCmd = require('skip'),
    musiqueCmd = require('musique'),
    pauseCmd = require('pause'),
    resumeCmd = require('resume'),
    stopCmd = require('stop'),
    volumeCmd = require('volume'),
    queueCmd = require('queue'),
    purgeCmd = require('purge'),
    joinCmd = require('join'),
    leaveCmd = require('leave')
}
